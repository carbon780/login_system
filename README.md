Run using venv Python directly : ".\.venv\Scripts\python.exe app.py"
Activate virtual environment : ".\.venv\Scripts\activate"
run python file directly : "python app.py"


intall extension sqlite viewer to see database.
run this in terminal before running app.py :-  "pip install -r requirements.txt"
